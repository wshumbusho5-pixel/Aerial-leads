[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements-va-app.txt"]

[start]
cmd = "streamlit run recruiting/application_page.py --server.port $PORT --server.address 0.0.0.0 --server.headless true"
